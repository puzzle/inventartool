<%= form_for(@entry) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this ram from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :capacity %><br />
    <%= f.text_field :capacity %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :machine_id %><br />
    <%= f.collection_select(:machine_id, @machines, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :machine_type %><br />
    <%= f.text_field :machine_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>